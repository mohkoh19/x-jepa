# @package _global_

defaults:
  - /experiment/pretraining/default
  - override /model: siglip

task_name: VL4M_siglip
tags: ["VL4M", "siglip", "baseline"]

data:
  # CLIP/SigLIP tokenize captions inside the model, so the collator only stacks
  # images and keeps the raw captions.
  collate_fn:
    _target_: src.data.collators.CaptionCollator
